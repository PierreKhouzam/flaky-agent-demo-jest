{
  "name": "flaky-agent-demo-jest",
  "version": "1.0.0",
  "description": "FLAKY-AGENT demo: Jest async race condition flakiness",
  "type": "commonjs",
  "scripts": {
    "test": "mkdir -p reports && (npx jest --json --outputFile=reports/test-results.json --forceExit || true)"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
